# 뜬다잉 v2 기획서

> 작성: 2026-07
> 목적: 취업 포트폴리오
> 개발 기간: 4주 (단기 집중)
>
> v1 개발 이력: 2025-05-13 최초 커밋 ~ 2025-08-30 마지막 커밋 (커밋 13개)

---

## 1. 한 문장 정의

**공항에 도착한 순간부터 탑승까지, 내 항공편 하나만 보여주는 앱.**

v1은 "공항 시간표를 보여주는 앱"이었다. v2는 "내 항공편을 보여주는 앱"이다.
시간표는 내 항공편을 찾기 위한 수단이지 목적이 아니다. 이 한 줄이 아래 모든 결정의 기준이 된다.

---

## 2. 대상 사용자와 사용 순간

### 대상 (v2 범위)

**국내 공항에서 비행기를 타려고 대기 중인 승객.**

### 사용 순간

| 시점 | 사용자 상태 | 앱이 해야 할 일 |
|---|---|---|
| 공항 도착 직후 | 탑승권은 있고, 게이트를 모름 | 탑승권 스캔 → 내 항공편 특정 |
| 대기 중 | 게이트에서 멀리 떨어져 밥 먹는 중 | 잠금화면에서 지연·게이트 변경 확인 |
| 탑승 직전 | 전광판 앞에 사람이 몰려 있음 | 앱 열자마자 게이트와 시각이 보임 |

세 순간의 공통점: **사용자는 항공편 하나만 궁금하다.** 나머지 40편은 노이즈다.

### v2에서 제외한 사용자

**마중·배웅하는 사람 (비승객).**

제외 이유:
- 탑승권이 없으므로 스캔 진입점을 쓸 수 없다 → 편명 검색이라는 별도 진입점이 필요
- 관심 대상이 도착편이다 → 데이터 축이 하나 더 늘어난다
- 결과적으로 화면 수가 두 배가 되는데, 4주 안에 두 축을 모두 완성도 있게 만들 수 없다

**단, 설계는 열어둔다.** `FlightRepository`의 조회 방향(`.departure` / `.arrival`)을 파라미터로 받는 형태로 만들어서, v1.1에서 도착편을 붙일 때 데이터 계층을 다시 쓰지 않아도 되게 한다.

---

## 3. 만들 것 — 화면 4개

### S1. 홈 (항공편 리스트)

앱의 기본 화면. 공항 하나의 출발편 목록.

- 상단: 내 항공편 카드 (저장된 게 있을 때만). 없으면 "탑승권을 스캔하세요" CTA
- 시간대별 섹션 그룹 (`14시대`, `15시대`)
- 현재 시각 기준선 — 캘린더 앱의 "지금" 라인과 동일한 장치
- 행 구성: 시각(가장 큼) → 목적지 → 항공사·편명·게이트(가장 작음)
- 지연 시 원래 시각에 취소선 + 변경 시각 병기
- 상태(탑승중/지연/결항)는 행 오른쪽 고정 위치

**핵심 구현 포인트**
- 모든 시각 표기에 `.monospacedDigit()` — 고정폭 숫자가 아니면 세로 정렬이 깨진다
- 로딩 / 에러 / 빈 결과 3가지 상태를 모두 화면으로 정의한다 (v1에는 `print()`만 있었다)
- 리스트 아이템 ID는 `편명 + 운항일` 조합. `UUID()`를 쓰면 새로고침마다 전체가 새 항목으로 인식된다

### S2. 탑승권 스캔

PDF417 바코드를 읽어 항공편을 자동 특정한다.

- 카메라 프리뷰 + 인식 영역 가이드
- 인식 성공 → BCBP 파싱 → 편명·구간·좌석 추출
- 추출한 편명으로 오늘 운항 목록에서 매칭 → 매칭 성공 시 자동 저장 후 홈으로 복귀
- 매칭 실패 시(운항 목록에 없는 편) 파싱 결과만 보여주고 수동 선택 유도

**이 화면이 이 앱의 차별점이다.** 사용자가 아무것도 입력하지 않는다.

### S3. 공항 선택

- 앱 최초 실행 시 위치 권한 요청 → 공항 반경 안이면 자동 선택
- 위치를 못 쓰면 목록에서 선택
- 이후 홈 상단 타이틀 탭으로 언제든 전환

**온보딩 슬라이드는 만들지 않는다.** 이 화면이 온보딩을 겸한다.

### S4. 항공편 상세

리스트에서 행을 탭했을 때.

- 예정 시각 / 변경 시각 / 게이트 / 터미널 / 상태
- 경유지 정보 (`VIA`가 있을 때만)
- "내 항공편으로 저장" 버튼

---

## 4. 앱 밖 산출물 2개

### W1. Live Activity (잠금화면 + 다이나믹 아일랜드)

내 항공편이 저장돼 있고 출발 3시간 이내일 때 활성화.

- 표시 정보: 편명, 목적지, 출발 시각, 게이트, 남은 시간
- 갱신 방식: **앱 실행 중 갱신 + Background App Refresh**
- APNs 푸시 갱신은 v2 범위 밖. 단, `ActivityKit`의 push token 발급 지점만 코드에 남겨두고 주석으로 확장 계획을 명시한다

### W2. 홈 화면 위젯

내 항공편의 게이트와 시각을 한 줄로. Small / Medium 두 사이즈.

> v1에서 위젯 익스텐션 타겟을 한 번 만들었다가(`4b54eee`, 2025-08-30) 템플릿 상태로 방치 후 제거했다.
> v2에서는 데이터 계층이 정리된 뒤 마지막 주에 새로 추가한다.

---

## 5. 만들지 않을 것 (명시적 제외)

이 목록이 기능 목록보다 중요하다. 면접에서 "왜 안 넣었나"에 답하기 위한 근거다.

| 제외 항목 | 이유 |
|---|---|
| 도착편 조회 | 대상 사용자(승객)의 사용 순간에 필요 없다. v1.1로 이연 |
| 마중·배웅 사용자용 편명 검색 | 위와 동일 |
| 다국어 (영/중/일) | 앱 자체는 한국인 대상. 단 API 응답의 다국어 필드는 모델에 보존해두고 표시만 안 한다 |
| APNs 푸시 서버 | 4주 안에 백엔드까지는 불가능. 구조만 열어둔다 |
| 지방 소규모 공항 (원주·사천·군산 등) | 편수가 적어 검증 가치가 낮다. 국제공항 6곳 + 포항경주만 |
| 항공기 실시간 위치 지도 | ADS-B 데이터가 필요하고 유료다. 개인 프로젝트 범위 밖 |
| 회원가입 / 계정 | 저장할 게 항공편 하나뿐이다. 로컬 저장으로 충분 |
| 항공권 예약 연동 | 앱의 정의("공항에 도착한 순간부터")를 벗어난다 |

---

## 6. 기술 결정과 그 이유

면접에서 물어볼 항목들이다. 각 결정마다 "왜"를 준비해둔다.

### 6.1 데이터 소스: 공공데이터포털로 전환

v1은 `airport.co.kr/gimpo/ajaxf/...` 라는 **한국공항공사 웹사이트의 내부 AJAX 엔드포인트**를 호출했다. 공개 계약이 없어 예고 없이 차단되거나 스펙이 바뀔 수 있고, 앱스토어 배포 시 문제가 될 소지도 있다.

전환 대상:
- 한국공항공사 — [항공기 운항정보](https://www.data.go.kr/data/15000126/openapi.do), [실시간 항공운항 현황 상세조회](https://www.data.go.kr/data/15113771/openapi.do)
- 인천국제공항공사 — 별도 개방 API

### 6.2 두 공항공사 API의 통합

한국공항공사와 인천국제공항공사는 별개 기관이고 응답 스펙이 다르다. 이 프로젝트에서 가장 설명할 가치가 있는 제약이다.

```
FlightRepository (protocol)
├── KACFlightRepository   — 한국공항공사 (인천 외 전 공항)
└── IIACFlightRepository  — 인천국제공항공사 (ICN)
```

- 도메인 모델 `Flight`는 두 API 어느 쪽에서 왔는지 모른다
- 각 Repository가 자기 DTO를 도메인 모델로 변환한다
- ViewModel은 프로토콜만 안다 → 테스트 시 Mock 주입 가능

### 6.3 아키텍처: MVVM + Repository

v1은 `FlightViewModel.fetchFlights()` 안에서 URL 조립, 네트워크 호출, 디코딩, 상태 갱신을 모두 했고 `URLSession.shared`에 직접 묶여 있었다. 그래서 **디코딩만 따로 테스트할 방법이 없었다.**

계층 분리:

```
Presentation  — View, ViewModel
Domain        — Flight, BoardingPass, UseCase
Data          — Repository 구현, DTO, NetworkClient(protocol)
```

### 6.4 테스트 전략

교체 전에 **현재 동작을 고정하는 특성화 테스트(Characterization Test)** 를 먼저 작성한다.
v1의 비즈니스 규칙은 코드 외에 어디에도 문서화돼 있지 않기 때문이다.

고정해야 할 규칙:
- 조회 범위는 현재 시각 ±N시간, 자정을 넘으면 `0000` / `2359`로 클램프
- `STD != ETD`이면 지연으로 간주
- 상태 문자열에 "사전결항" 포함 시 별도 표시

테스트 대상 (네트워크 없이 순수 로직만):
- BCBP 파서 — 정상 / 길이 부족 / 좌석 미배정 케이스
- 시간 윈도우 계산 — 자정 전후 경계
- DTO → 도메인 변환 — 저장된 실제 응답 JSON 픽스처 사용

### 6.5 저장

v1은 선택한 항공편을 `@State`로만 들고 있어서 앱을 끄면 사라졌다. 이 앱의 핵심 가치인데도.
→ SwiftData로 영속화. 위젯·Live Activity와 공유해야 하므로 App Group 설정 필요.

---

## 7. 4주 실행 계획

### 0주차 — 시작 전

**저장소 정리 (완료)**
- [x] `.gitignore` 추가, `xcuserdata` / `.DS_Store` 추적 해제
- [x] 미사용 `FlightsWidget` 타겟 제거
- [ ] 상위 폴더의 빈 저장소(`~/Developer/.../PersonalProject/ddendaing/.git`) 제거
      — 원격이 연결돼 있지 않은 껍데기. 중첩 저장소 상태라 사고 위험이 있다

**데이터 확보 (시간에 민감)**
- [ ] **현재 API의 실제 응답 JSON을 케이스별로 저장** — 이 엔드포인트는 비공식이라 언제 막힐지 모른다
  - 지연 있는 편 / 결항 편 / `GATE`가 null인 편 / 결과가 빈 시간대(새벽) / 경유편
  - 저장 위치: `Tests/Fixtures/`
- [ ] 공공데이터포털 API 키 신청 (승인에 1~2일 걸릴 수 있으므로 가장 먼저)

### 1주차 — 데이터 계층

- 도메인 모델 `Flight` 재정의 (안정적 ID: 편명 + 운항일)
- `NetworkClient` 프로토콜 분리 → `URLSession` 의존성 제거
- `FlightRepository` 프로토콜 + `KACFlightRepository` 구현
- 저장해둔 JSON 픽스처로 디코딩 테스트 작성

**완료 기준:** 네트워크 없이 테스트가 돌고, 앱이 새 API로 리스트를 그린다

### 2주차 — 화면 재설계

- S1 홈 리스트 (시간대 그룹, 현재 시각 기준선, 3단 위계, monospaced digit)
- 로딩 / 에러 / 빈 결과 상태
- S3 공항 선택 + 위치 기반 자동 감지
- S4 상세

**완료 기준:** v1보다 명백히 읽기 쉬운 화면. before/after 스크린샷 확보

### 3주차 — 스캔과 저장

- `QRCodeScannerView`의 **이중 `AVCaptureSession` 버그 수정**
  (v1은 `ScannerViewModel`과 `ScannerViewController`가 각각 세션을 만들어 동시 실행했다)
- BCBP 파서 테스트 작성 + 리팩터링
- 스캔 결과 → 운항 목록 매칭 로직
- SwiftData 저장 + App Group

**완료 기준:** 탑승권을 스캔하면 홈 상단에 내 항공편이 뜨고, 앱을 껐다 켜도 남아 있다

### 4주차 — Live Activity와 마무리

- Live Activity (잠금화면 + 다이나믹 아일랜드)
- 홈 화면 위젯 Small / Medium
- `IIACFlightRepository` (인천공항) 추가
- README 작성 — 문제 정의, 아키텍처 다이어그램, before/after, 기술 결정과 근거

**완료 기준:** 면접에서 폰을 꺼내 잠금화면을 보여줄 수 있다

---

## 8. 위험 구간과 잘라낼 후보

4주는 빡빡하다. 밀릴 경우를 대비해 **자를 순서를 미리 정해둔다.**

| 순위 | 잘라낼 항목 | 근거 |
|---|---|---|
| 1 | 홈 화면 위젯 | Live Activity와 정보가 중복된다. 임팩트 대비 작업량이 크다 |
| 2 | 인천공항 Repository | 프로토콜 구조로 "확장 가능함"을 이미 보여줄 수 있다. 구현체는 하나여도 설명이 성립한다 |
| 3 | S4 상세 화면 | 리스트 + 내 항공편 카드로 대부분의 정보가 이미 보인다 |

**절대 자르지 않을 것:** API 전환, 테스트, 스캔 → 저장 흐름, README.
이 넷이 이 프로젝트의 서사 전부다.

---

## 9. 성공 기준

이 프로젝트가 성공했는지 판단하는 기준은 사용자 수가 아니다.

- [ ] 커밋 히스토리에 "무엇을 왜 바꿨는지"가 남아 있다
- [ ] 네트워크 없이 도는 테스트가 있고, 실제로 통과한다
- [ ] "왜 이 API를 썼나" "왜 이렇게 나눴나" "왜 이건 안 만들었나" 세 질문에 각각 2분씩 답할 수 있다
- [ ] 2025년 코드와 2026년 코드의 차이를 스스로 설명할 수 있다
- [ ] 면접장에서 폰을 꺼내 30초 안에 데모할 수 있다

---

## 부록 A: v1.1 이후 후보

범위 밖이지만 확장 지점으로 기록해둔다.

- 도착편 조회 + 마중·배웅 사용자용 편명 검색
- APNs 기반 Live Activity 원격 갱신
- 다국어 지원 (API 응답에 이미 4개 언어가 들어온다)
- 게이트 변경 시 로컬 알림

## 부록 B: 작업 규칙

- 커밋 메시지는 `git commit -m "제목" -m "본문"` 형태로 작성한다
  (여러 줄 문자열을 한 번에 넘기면 편집기가 열려 사고가 난다)
- 강제 푸시가 필요하면 `--force` 대신 반드시 `--force-with-lease` 를 쓴다
- 리팩터링은 문제 단위로 쪼개서 커밋한다. 커밋 히스토리 자체가 포트폴리오다
